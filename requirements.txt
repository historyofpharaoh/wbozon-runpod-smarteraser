# Зависимости для RORem (SDXL-inpaint fine-tune) на Runpod Serverless.
# torch и torchvision ставятся отдельно через --extra-index-url cu128 (см. Dockerfile)
# — нужны kernels для Blackwell GPU sm_120 которые Runpod подсовывает как fallback.
diffusers==0.30.0
transformers==4.46.3
accelerate==1.0.0
safetensors==0.4.5
huggingface_hub==0.24.5
numpy==1.26.4
Pillow==10.4.0
opencv-python==4.10.0.84
requests==2.32.3
runpod==1.7.0
