# Зависимости для RORem (SDXL-inpaint fine-tune) на Runpod Serverless.
# torch 2.5+ обязательно — нужны CUDA kernels для Blackwell GPU (RTX PRO 6000, sm_120),
# которые Runpod может подсунуть как fallback на пиках. torch 2.1.2 их не знает →
# RuntimeError: CUDA error: no kernel image is available for execution on the device.
torch==2.5.1
torchvision==0.20.1
diffusers==0.30.0
transformers==4.46.3
accelerate==1.0.0
safetensors==0.4.5
huggingface_hub==0.24.5
numpy==1.26.4
Pillow==10.4.0
opencv-python==4.10.0.84
requests==2.32.3
runpod==1.7.0
